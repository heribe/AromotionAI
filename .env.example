# AromotionAI 环境变量说明
#
# ⚠️ 本文件仅为说明，部署时无需在根目录创建 .env。
#
# docker compose 直接读取 backend/.env（见 docker-compose.yml 的 env_file），
# 与本地开发共用同一份配置，路径统一，无需为部署单独复制凭据。
#
# 配置步骤：
#   1. 复制 backend/.env.example → backend/.env，填入 GLM_API_KEY
#   2. 把抖音 cookie 放到 backend/data/cookies/douyin.json
#   3. docker compose up -d --build
#
# 完整变量说明见 backend/.env.example。
