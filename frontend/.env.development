# 开发环境配置（npm run dev 时加载）
# API 基础路径（相对路径，由 vite.config.ts 的 server.proxy 转发到后端）
VITE_API_BASE=/api/v1
# 是否使用 Mock 数据（true=纯前端预览无需后端；false=走真实后端）
VITE_USE_MOCK=false
# 后端地址（仅供 vite.config.ts 的 proxy target 读取）
VITE_BACKEND_URL=http://localhost:8000
